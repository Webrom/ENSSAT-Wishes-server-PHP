- PROMPT DE CONFIRMATION POUR CHAQUE SUPPRESSION
- ADMIN PEUT MODIFIER LES INFORMATIONS DE CHAQUE PERSONNE QUI S'INSCRIT (a reprendre : modifs des modules)
- VALIDATION ADMIN (pourquoi ça marche pas chez ouam)
- Dans gestion users : pouvoir afficher tous les users inactifs
- Export CVS









---- COLIN
- Fil de recherche par promotion / par semestre
- Les interfaces de consultation devront permettre de visualiser les données sous
différents angles (par exemple par (ou pour un) semestre, par (ou pour une) formation)
à différents niveaux d’abstraction.


        if(!$this->session->userdata('is_logged_in')){
            redirect('login');
        }else { ...
        ailleurs ?